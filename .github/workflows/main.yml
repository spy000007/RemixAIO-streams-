name: Build Android APK

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3

    - name: Prepare Google AI Studio Project
      run: |
        # 1. Create gradle directory and move version catalog completely
        mkdir -p gradle
        if [ -f "libs.versions.toml" ]; then
          mv libs.versions.toml gradle/
        fi

        # 2. Setup .env file
        if [ -f ".env.example" ]; then
          cp .env.example .env
        else
          echo "GEMINI_API_KEY=dummy_key_for_ci_build" > .env
        fi

        # 3. Clean settings.gradle.kts for Single Root Project
        cat << 'EOF' > settings.gradle.kts
        pluginManagement {
            repositories {
                google()
                mavenCentral()
                gradlePluginPortal()
            }
        }
        dependencyResolutionManagement {
            repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
            repositories {
                google()
                mavenCentral()
            }
        }
        rootProject.name = "RemixAIO-streams"
        EOF

        # 4. Remove debugConfig signing line as requested by AI Studio README
        if [ -f "build.gradle.kts" ]; then
          sed -i 's/signingConfig = signingConfigs.getByName("debugConfig")//g' build.gradle.kts
        fi

    - name: Build Debug APK
      run: gradle assembleDebug --stacktrace

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug
        path: "**/build/outputs/apk/debug/*.apk"
        
